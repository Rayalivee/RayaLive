:root {
  --bg: #FFFFFF;
  --surface: #FAFAFA;
  --surface-2: #F0F0F0;
  --border: #E4E4E4;
  --text: #111111;
  --muted: #7A7A7A;
  --ink: #111111;
  --font-display: 'Space Grotesk', sans-serif;
  --font-body: 'Inter', sans-serif;
}

* { box-sizing: border-box; }

body {
  margin: 0;
  background: var(--bg);
  color: var(--text);
  font-family: var(--font-body);
  min-height: 100vh;
}

.wrap {
  max-width: 640px;
  margin: 0 auto;
  padding: 28px 20px 60px;
}

.top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 40px;
}

.brand { display: flex; align-items: center; gap: 10px; }

.brand-mark {
  display: inline-block;
  width: 34px;
  height: 34px;
  background: var(--surface-2);
  border: 1px solid var(--border);
  border-radius: 10px;
}

.brand-name {
  font-family: var(--font-display);
  font-weight: 700;
  font-size: 18px;
  letter-spacing: -0.01em;
}

.user-chip {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 6px 12px;
  color: var(--text);
  cursor: pointer;
  font-family: var(--font-body);
  transition: border-color 0.2s ease;
}
.user-chip:hover { border-color: var(--ink); }
#user-name-label { font-size: 13px; font-weight: 600; }
.user-id { font-size: 11px; color: var(--muted); }
.user-streak { font-size: 11px; color: var(--muted); margin-top: 1px; }

.view.hidden { display: none; }
.view {
  animation: fadeIn 0.4s ease;
}
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(8px); }
  to { opacity: 1; transform: translateY(0); }
}

.headline {
  font-family: var(--font-display);
  font-size: 30px;
  line-height: 1.2;
  font-weight: 700;
  margin: 0 0 10px;
  letter-spacing: -0.01em;
}
.headline.small { font-size: 22px; }

.sub {
  color: var(--muted);
  font-size: 15px;
  max-width: 46ch;
  margin: 0 0 28px;
  line-height: 1.55;
}
.sub.small { margin-bottom: 18px; }

.menu-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 12px;
}

.option-card {
  text-align: left;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 18px;
  padding: 18px 20px;
  color: var(--text);
  cursor: pointer;
  display: flex;
  flex-direction: column;
  gap: 4px;
  transition: border-color 0.2s ease, transform 0.15s ease, background 0.2s ease;
  font-family: var(--font-body);
}
.option-card:hover { border-color: var(--ink); background: var(--surface-2); transform: translateY(-1px); }
.option-card:active { transform: translateY(0); }

.option-title {
  font-family: var(--font-display);
  font-weight: 700;
  font-size: 17px;
}
.option-desc { color: var(--muted); font-size: 13.5px; }

.inline-panel {
  margin-top: 18px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 18px;
  padding: 18px 20px;
  animation: fadeIn 0.3s ease;
}
.inline-panel.hidden { display: none; }

.inline-panel label {
  display: block;
  font-size: 13px;
  color: var(--muted);
  margin-bottom: 8px;
}

.inline-row { display: flex; gap: 10px; }

input {
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 11px 14px;
  color: var(--text);
  font-family: var(--font-body);
  font-size: 15px;
  width: 100%;
  letter-spacing: 0.04em;
}
input:focus { outline: 2px solid var(--ink); outline-offset: 1px; }

.btn-primary {
  background: var(--ink);
  color: #FFFFFF;
  border: none;
  border-radius: 999px;
  padding: 11px 20px;
  font-weight: 600;
  font-family: var(--font-body);
  cursor: pointer;
  font-size: 14.5px;
  white-space: nowrap;
  transition: opacity 0.2s ease;
}
.btn-primary:hover { opacity: 0.82; }
.btn-primary.full { width: 100%; margin-top: 14px; }

.btn-secondary {
  background: transparent;
  border: 1px solid var(--ink);
  color: var(--ink);
  border-radius: 999px;
  padding: 10px 18px;
  cursor: pointer;
  font-family: var(--font-body);
  font-weight: 600;
  font-size: 14px;
  transition: background 0.2s ease;
}
.btn-secondary:hover { background: var(--surface-2); }

.btn-ghost {
  background: transparent;
  border: none;
  color: var(--muted);
  cursor: pointer;
  font-family: var(--font-body);
  font-size: 14px;
  padding: 8px 4px;
  text-decoration: underline;
  text-underline-offset: 3px;
}
.btn-ghost:hover { color: var(--text); }

.error-text { color: var(--text); font-weight: 600; font-size: 13px; margin: 10px 0 0; min-height: 16px; }

.pill-row { display: flex; gap: 8px; margin-bottom: 4px; }
.pill {
  background: var(--bg);
  border: 1px solid var(--border);
  color: var(--muted);
  border-radius: 999px;
  padding: 7px 14px;
  font-size: 13px;
  cursor: pointer;
  font-family: var(--font-body);
  transition: border-color 0.2s ease, background 0.2s ease, color 0.2s ease;
}
.pill.active { border-color: var(--ink); color: #fff; background: var(--ink); }

#view-waiting { text-align: center; }
.room-code {
  font-family: var(--font-display);
  font-size: 42px;
  font-weight: 700;
  letter-spacing: 0.14em;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 18px;
  padding: 20px;
  margin: 4px 0 18px;
}
.hint { color: var(--muted); font-size: 13px; margin-top: 18px; }

.game-head { margin-bottom: 22px; }
.players-row {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 14px;
  margin-bottom: 12px;
}
.player-tag {
  display: flex;
  align-items: center;
  gap: 9px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 8px 14px;
}
.sym { font-family: var(--font-display); font-weight: 700; font-size: 15px; }
.pinfo { display: flex; flex-direction: column; line-height: 1.25; }
.pname { font-size: 13.5px; color: var(--text); }
.streak { font-size: 10.5px; color: var(--muted); min-height: 13px; }
.vs { color: var(--muted); font-size: 12px; }

.status-line {
  text-align: center;
  color: var(--muted);
  font-size: 14px;
  margin: 0 0 10px;
}

.timer-wrap {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  max-width: 240px;
  margin: 0 auto;
}
.timer-track {
  flex: 1;
  height: 5px;
  background: var(--surface-2);
  border-radius: 999px;
  overflow: hidden;
}
.timer-fill {
  height: 100%;
  width: 100%;
  background: var(--ink);
  border-radius: 999px;
  transition: width 0.25s linear, background 0.25s ease;
}
.timer-fill.warn { animation: pulse 0.6s ease infinite; }
@keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.35; } }
.timer-value { font-size: 12px; color: var(--muted); min-width: 26px; text-align: right; }

.board {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 10px;
  background: #C4C4C4;
  padding: 10px;
  border-radius: 22px;
  max-width: 340px;
  margin: 0 auto;
  aspect-ratio: 1;
}

.cell {
  background: var(--surface);
  border: none;
  border-radius: 14px;
  font-family: var(--font-display);
  font-size: 40px;
  font-weight: 700;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: background 0.15s ease;
  color: var(--ink);
}
.cell:hover:not(:disabled) { background: var(--surface-2); }
.cell:disabled { cursor: default; }
.cell.filled { animation: pop 0.25s ease; }
@keyframes pop {
  from { opacity: 0; transform: scale(0.7); }
  to { opacity: 1; transform: scale(1); }
}
.cell.win { background: var(--ink); color: #fff; }

.game-actions {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  margin-top: 26px;
}

.modal {
  position: fixed;
  inset: 0;
  background: rgba(17,17,17,0.45);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20px;
  z-index: 10;
  animation: fadeIn 0.25s ease;
}
.modal.hidden { display: none; }
.modal-card {
  background: #fff;
  border: 1px solid var(--border);
  border-radius: 20px;
  padding: 24px;
  max-width: 340px;
  width: 100%;
}
.modal-card h3 { font-family: var(--font-display); margin: 0 0 6px; }
.modal-card input { margin: 10px 0; }

.result-title {
  font-family: var(--font-display);
  font-size: 26px;
  font-weight: 700;
  margin: 0 0 14px;
}
.result-title.win,
.result-title.lose {
  color: #D6304A;
}

.rematch-box { margin-bottom: 14px; }
.rematch-box.hidden { display: none; }
.rematch-actions { display: flex; gap: 10px; margin-top: 10px; }
.rematch-actions .btn-primary,
.rematch-actions .btn-secondary { flex: 1; }
#rematch-ask-btn.hidden { display: none; }

.toast {
  position: fixed;
  left: 50%;
  bottom: 26px;
  transform: translate(-50%, 12px);
  background: var(--ink);
  color: #fff;
  padding: 10px 20px;
  border-radius: 999px;
  font-size: 13.5px;
  opacity: 0;
  transition: opacity 0.25s ease, transform 0.25s ease;
  z-index: 50;
  max-width: 90vw;
  text-align: center;
}
.toast.show { opacity: 1; transform: translate(-50%, 0); }

.ranking-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 2px;
}

.ranking-list {
  list-style: none;
  margin: 18px 0 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.ranking-row {
  display: flex;
  align-items: center;
  gap: 12px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 14px;
  padding: 10px 16px;
}
.ranking-row.me { border-color: var(--ink); }

.ranking-pos {
  font-family: var(--font-display);
  font-weight: 700;
  width: 32px;
  color: var(--muted);
  font-size: 13px;
}
.ranking-name { flex: 1; font-size: 14px; }
.ranking-streak { font-size: 13.5px; font-weight: 600; white-space: nowrap; }

.achievements-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
  margin-top: 18px;
}

.achievement-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 14px 16px;
  transition: opacity 0.2s ease;
}
.achievement-card.locked { opacity: 0.5; }

.achievement-card-head {
  display: flex;
  align-items: center;
  gap: 12px;
}
.achievement-icon { font-size: 22px; flex-shrink: 0; }
.achievement-item-text { display: flex; flex-direction: column; gap: 2px; }
.achievement-item-title { font-size: 13.5px; font-weight: 600; }
.achievement-item-desc { font-size: 12px; color: var(--muted); }

.achievement-toast {
  position: fixed;
  top: 18px;
  right: 18px;
  display: flex;
  align-items: center;
  gap: 10px;
  background: var(--ink);
  color: #fff;
  border-radius: 16px;
  padding: 12px 16px;
  max-width: min(320px, calc(100vw - 36px));
  opacity: 0;
  transform: translateX(24px);
  transition: opacity 0.3s ease, transform 0.3s ease;
  z-index: 60;
  box-shadow: 0 10px 26px rgba(0, 0, 0, 0.2);
}
.achievement-toast.show { opacity: 1; transform: translateX(0); }
.achievement-toast-icon { font-size: 22px; flex-shrink: 0; }
.achievement-toast-text {
  display: flex;
  flex-direction: column;
  gap: 2px;
  min-width: 0;
}
.achievement-toast-kicker {
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: #C9C9C9;
}
.achievement-toast-title {
  font-size: 13.5px;
  font-weight: 600;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.modal-stats {
  margin-top: 18px;
  padding-top: 16px;
  border-top: 1px solid var(--border);
}
.modal-stats-title {
  font-size: 12px;
  font-weight: 600;
  color: var(--muted);
  margin: 0 0 10px;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}

.stats-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 8px;
}
.stat-cell {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2px;
  background: var(--surface-2);
  border-radius: 12px;
  padding: 10px 6px;
}
.stat-value {
  font-family: var(--font-display);
  font-weight: 700;
  font-size: 18px;
}
.stat-label {
  font-size: 10px;
  color: var(--muted);
  text-align: center;
}

.pname.clickable-name {
  cursor: pointer;
  text-decoration: underline;
  text-decoration-color: var(--border);
  text-underline-offset: 2px;
}
.pname.clickable-name:hover { text-decoration-color: var(--ink); }

.banned-screen {
  position: fixed;
  inset: 0;
  background: #0A0A0F;
  color: #fff;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 200;
  padding: 24px;
}
.banned-screen.hidden { display: none; }
.banned-card { text-align: center; max-width: 360px; }
.banned-title {
  font-family: var(--font-display);
  font-size: 42px;
  font-weight: 700;
  color: #D6304A;
  letter-spacing: 0.06em;
  margin: 0 0 24px;
}
.banned-label {
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: #8E8E99;
  margin: 16px 0 4px;
}
.banned-text { font-size: 15px; margin: 0; }

.welcome-version {
  display: inline-block;
  background: var(--surface-2);
  border-radius: 999px;
  padding: 4px 12px;
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.04em;
  color: var(--muted);
  margin: 0 0 10px;
}

.coins-badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 999px;
  padding: 6px 14px;
  font-size: 13px;
  font-weight: 600;
  margin: 4px 0 18px;
}

.store-list {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.store-item {
  display: flex;
  align-items: center;
  gap: 12px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 12px 16px;
}

.store-swatch {
  width: 26px;
  height: 26px;
  border-radius: 50%;
  border: 1px solid var(--border);
  flex-shrink: 0;
}

.store-info {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 2px;
  min-width: 0;
}
.store-name { font-size: 13.5px; font-weight: 600; }
.store-price { font-size: 12px; color: var(--muted); }

.store-item .btn-primary,
.store-item .btn-secondary {
  padding: 8px 14px;
  font-size: 13px;
  white-space: nowrap;
  flex-shrink: 0;
}
.store-item .btn-primary:disabled {
  opacity: 0.4;
  cursor: default;
}

.store-section-title {
  font-family: var(--font-display);
  font-size: 15px;
  font-weight: 700;
  margin: 26px 0 4px;
}

.store-swatch-random {
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--surface-2);
  font-size: 14px;
}

.vip-name {
  color: #D6304A;
  font-weight: 700;
}

@media (min-width: 560px) {
  .headline { font-size: 36px; }
}
